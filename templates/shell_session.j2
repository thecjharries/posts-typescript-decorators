{% macro shell_session(contents) %}

{{
    highlight_block(
        contents,
        raw = contents,
        linenos = False,
        explicit_lexer_name = 'BashSessionLexer',
        no_header = True
    )
}}

{% endmacro %}
